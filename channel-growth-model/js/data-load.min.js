Object.keys(modelData.pillars).forEach(function(key){var pillarData=modelData.pillars[key];$("#"+key+"-average").css("width",pillarData.average+"%");var nextSteps=$("#"+key+"-next-steps");var nextStepCount=0;for(var i=0;i<pillarData.resources.length;++i){var resource=pillarData.resources[i];var input=$("#"+key+"-"+resource.position+"-input");var label=$("#"+key+"-"+resource.position+"-label");input.attr("data-index",resource.index);input.attr("data-score",resource.score);input.attr("value",resource.label);label.text(resource.label)}});